{
  "devDependencies": {
    "vitepress": "^1.6.4"
  },
  "scripts": {
    "docs:dev": "npm run docs:prepare && vitepress dev docs",
    "docs:build": "npm run docs:prepare && vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "docs:prepare": "node scripts/sync-docs.mjs"
  },
  "name": "luna-flow-docs",
  "private": true,
  "description": "Documentation site for the LunaFlow MoonBit ecosystem, with package guides, API entry points, and multilingual project pages."
}
